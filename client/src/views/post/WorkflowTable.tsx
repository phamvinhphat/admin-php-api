import { Table, TableContainer } from '@mui/material';

const WorkflowTable = () => {
    return (
        <TableContainer>
            <Table>

            </Table>
        </TableContainer>
    );
};

export default WorkflowTable;
